{% extends 'base.html.twig' %}

{% block title %}Blog - Ajout{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <h1>Ajout d'un nouveau post.</h1>
            </div>
            <div class="row">
                <div class="col-lg-6">
                {{ form_start(form) }}
                    {{ form_rest(form) }}
                    <button type="submit" class="btn btn-primary">ajouter</button>
                    <a href="{{ path('blog') }}" class="btn btn-danger">Annuler</a>
                {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}