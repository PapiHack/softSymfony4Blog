{% extends 'base.html.twig' %}

{% block title %}Blog - Modif{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <h1>Modif d'un post.</h1>
            </div>
            <div class="row">
                <div class="col-lg-6">
                {{ form_start(form) }}
                    {{ form_rest(form) }}
                    <button type="submit" class="btn btn-primary">Modifier</button>
                    <a href="{{ path('blog') }}" class="btn btn-danger">Annuler</a>
                {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}